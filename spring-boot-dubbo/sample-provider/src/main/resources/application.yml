spring:
  application:
    name: spring-boot-dubbo

dubbo:
  application:
    name: spring-boot-provider
  protocol:
    name: dubbo
    port: 20880
  registry:
    address: zookeeper://localhost:2181
